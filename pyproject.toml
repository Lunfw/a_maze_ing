[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "mazegen-nocurely-frasomal"
version = "1.0.0"
description = "A maze generator"
requires-python = ">=3.10"
dependencies = ["pyfiglet"]

[tool.setuptools.packages.find]
where = ["src"]